import { Routes } from '@angular/router';
import { TrainingComponent } from './training.component';

export const TRAINING_ROUTES: Routes = [
  { path: '', component: TrainingComponent, title: 'Training & Support' }
];

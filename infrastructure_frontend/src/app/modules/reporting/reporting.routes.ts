import { Routes } from '@angular/router';
import { ReportingComponent } from './reporting.component';

export const REPORTING_ROUTES: Routes = [
  { path: '', component: ReportingComponent, title: 'Reporting' }
];

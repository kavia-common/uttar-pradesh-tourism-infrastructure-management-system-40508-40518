import { Routes } from '@angular/router';
import { PaymentsComponent } from './payments.component';

export const PAYMENTS_ROUTES: Routes = [
  { path: '', component: PaymentsComponent, title: 'Payments' }
];

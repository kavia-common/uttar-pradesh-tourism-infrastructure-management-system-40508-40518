import { Routes } from '@angular/router';
import { ProgressComponent } from './progress.component';

export const PROGRESS_ROUTES: Routes = [
  { path: '', component: ProgressComponent, title: 'Progress (Maps)' }
];

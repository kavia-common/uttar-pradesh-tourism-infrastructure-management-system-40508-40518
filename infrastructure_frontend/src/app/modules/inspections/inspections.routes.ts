import { Routes } from '@angular/router';
import { InspectionsComponent } from './inspections.component';

export const INSPECTIONS_ROUTES: Routes = [
  { path: '', component: InspectionsComponent, title: 'Inspections & Handover' }
];
